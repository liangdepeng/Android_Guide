ext {

    android = [

    ]

    // 是否模块单独编译 组件化开发业务module单独运行开关
    // Whether the module is compiled separately, the component-based development business module is operated separately
    isAloneBuild = false
}