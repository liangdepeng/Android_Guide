<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".down.DownloadActivity">

    <EditText
        android:id="@+id/in_et"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:hint="输入下载文件的链接"
        android:textSize="14sp"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/download_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:text="下载(单线程)"
        app:layout_constraintTop_toBottomOf="@+id/in_et" />

    <EditText
        android:id="@+id/in_et2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="100dp"
        android:layout_marginRight="10dp"
        android:hint="输入下载文件的链接"
        android:textSize="14sp"
        app:layout_constraintTop_toBottomOf="@+id/download_btn" />

    <Button
        android:id="@+id/download_mutli_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        android:text="下载(多线程)"
        app:layout_constraintTop_toBottomOf="@+id/in_et2" />

</androidx.constraintlayout.widget.ConstraintLayout>